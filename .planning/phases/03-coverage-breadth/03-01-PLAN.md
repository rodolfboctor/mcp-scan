---
phase: 03-coverage-breadth
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified: [src/config/paths.ts, src/config/detector.ts, tests/config/detector.test.ts]
autonomous: true
requirements: [COV-01, COV-02, COV-03, COV-04, COV-05, COV-06, COV-07, COV-08]
must_haves:
  truths:
    - "mcp-scan detects Zed config at ~/.config/zed/settings.json"
    - "mcp-scan detects Continue.dev config at ~/.continue/config.json"
    - "mcp-scan detects Cline config using glob for extension folder"
    - "mcp-scan detects Roo Code config using glob for extension folder"
    - "mcp-scan detects Amp, Plandex, and GitHub Copilot configs"
    - "mcp-scan detects ChatGPT Desktop config on macOS"
  artifacts:
    - path: "src/config/paths.ts"
      provides: "Expanded list of tool config paths"
    - path: "src/config/detector.ts"
      provides: "Glob-aware tool detection logic"
  key_links:
    - from: "src/config/detector.ts"
      to: "src/config/paths.ts"
      via: "getConfigPaths() call"
---

<objective>
Expand mcp-scan coverage to detect 8 new AI tools, including support for directory globs for VS Code extensions (Cline, Roo Code).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-coverage-breadth/03-CONTEXT.md
@src/config/paths.ts
@src/config/detector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new tool paths to src/config/paths.ts</name>
  <read_first>
    - src/config/paths.ts
    - .planning/phases/03-coverage-breadth/03-CONTEXT.md
  </read_first>
  <files>src/config/paths.ts</files>
  <action>
    Add the following tools and their paths to the `paths` object in `getConfigPaths()` for all platforms where applicable:
    - Zed: `path.join(home, '.config', 'zed', 'settings.json')` (for darwin/linux)
    - Continue: `path.join(home, '.continue', 'config.json')`
    - Cline: `path.join(home, '.vscode', 'extensions', 'saoudrizwan.claude-dev*', 'settings.json')`
    - Roo Code: `path.join(home, '.vscode', 'extensions', 'rooveterinaryinc.roo-cline*', 'settings.json')`
    - Amp: `path.join(home, '.amp', 'config.json')`
    - Plandex: `path.join(home, '.plandex', 'config.json')`
    - ChatGPT Desktop: `path.join(home, 'Library', 'Application Support', 'com.openai.chat', 'settings.json')` (macOS only)
    - GitHub Copilot: `path.join(home, '.config', 'github-copilot', 'apps.json')`
    
    Ensure Windows-specific paths are also added using `userProfile` or `appData` where appropriate, following the existing platform pattern.
  </action>
  <verify>
    <automated>grep -E "Zed|Continue|Cline|Roo Code|Amp|Plandex|ChatGPT Desktop|GitHub Copilot" src/config/paths.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/paths.ts` contains definitions for all 8 tools
    - Paths use `path.join` and reference `home`, `appData`, or `userProfile` correctly
    - Cline and Roo Code paths include the `*` glob character
  </acceptance_criteria>
  <done>New tool paths are defined in `src/config/paths.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement glob-aware detection in src/config/detector.ts</name>
  <read_first>
    - src/config/detector.ts
    - src/config/paths.ts
  </read_first>
  <files>src/config/detector.ts</files>
  <action>
    Modify `detectTools()` in `src/config/detector.ts` to handle paths containing `*`.
    
    1. For each path from `getConfigPaths()`:
       - If the path does NOT contain `*`, use existing `fs.existsSync(resolved)`.
       - If the path DOES contain `*` (e.g., in a directory name):
         - Split the path by `path.sep`.
         - Find the first segment containing `*`.
         - Use `fs.readdirSync` on the parent directory to find matching directories.
         - For each match, check if the full resolved path exists.
         - Add all found matches to the `detected` list.
    
    Ensure `seenPaths` deduplication still works for resolved paths.
  </action>
  <verify>
    <automated>npm run build && npm test tests/config/detector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `detectTools` handles paths with `*` by searching the filesystem
    - Multiple matches for a glob path are all added to the `detected` list
    - Existing non-glob detection remains functional
  </acceptance_criteria>
  <done>`detectTools` correctly finds config files even when they are inside versioned folders (globs).</done>
</task>

<task type="auto">
  <name>Task 3: Verify tool detection with mocks</name>
  <read_first>
    - tests/config/detector.test.ts
    - src/config/detector.ts
  </read_first>
  <files>tests/config/detector.test.ts</files>
  <action>
    Add a new test suite to `tests/config/detector.test.ts` that:
    1. Mocks `os.homedir()` and `fs`.
    2. Simulates the existence of Zed, Continue, and a versioned Cline extension folder (e.g., `~/.vscode/extensions/saoudrizwan.claude-dev-1.0.0/settings.json`).
    3. Asserts that `detectTools()` returns all of them with `exists: true`.
    4. Verifies that `seenPaths` correctly deduplicates multiple references to the same file.
  </action>
  <verify>
    <automated>npm test tests/config/detector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass for mocked glob paths
    - Tests pass for regular tool paths
    - All 8 new tools are covered by detection tests
  </acceptance_criteria>
  <done>Tool detection is verified for glob and non-glob paths.</done>
</task>

</tasks>

<verification>
Run `mcp-scan` (or `node dist/index.js ls`) and verify it detects mocked configs for the new tools.
</verification>

<success_criteria>
- Zed, Continue, Cline, Roo Code, Amp, Plandex, ChatGPT Desktop, and GitHub Copilot are detectable by mcp-scan.
- Glob-based path detection works for VS Code extensions.
- No regressions in existing tool detection.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-breadth/03-01-SUMMARY.md`
</output>
