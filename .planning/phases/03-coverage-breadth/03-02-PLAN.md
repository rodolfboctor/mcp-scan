---
phase: 03-coverage-breadth
plan: 03-02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/index.ts, src/commands/scan.ts, package.json]
autonomous: true
requirements: [COV-09, CI-01]
must_haves:
  truths:
    - "mcp-scan scan --ci exits 1 if any findings (Critical to Info) exist"
    - "mcp-scan scan --ci exits 0 if no findings exist"
    - "mcp-scan detects .mcp.json in the current working directory"
    - "package.json version is bumped to 1.1.0"
  artifacts:
    - path: "src/index.ts"
      provides: "--ci flag definition"
    - path: "package.json"
      contains: "\"version\": \"1.1.0\""
  key_links:
    - from: "src/index.ts"
      to: "src/commands/scan.ts"
      via: "options.ci passing"
---

<objective>
Implement strict CI exit codes via the --ci flag and finalize Phase 03 with a version bump to 1.1.0.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-coverage-breadth/03-CONTEXT.md
@src/index.ts
@src/commands/scan.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --ci flag to scan command and implement exit logic</name>
  <read_first>
    - src/index.ts
    - src/commands/scan.ts
  </read_first>
  <files>src/index.ts, src/commands/scan.ts</files>
  <action>
    1. In `src/index.ts`, add `.option('--ci', 'CI mode: exit 1 on any findings')` to the `scan` command.
    2. Update the `scan` command's `.action` block to handle the `ci` option:
       - Calculate `totalFindings = report.criticalCount + report.highCount + report.mediumCount + report.lowCount + report.infoCount`.
       - If `options.ci` is true and `totalFindings > 0`, call `process.exit(1)`.
       - Keep the existing fallback exit logic for Critical/High findings if `options.ci` is false.
    3. Ensure `runScan` in `src/commands/scan.ts` accepts the `ci` option in its signature if needed (though it might be enough to handle it in `index.ts`).
  </action>
  <verify>
    <automated>node dist/index.js scan --help | grep "\-\-ci"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp-scan scan --help` shows the `--ci` flag
    - `src/index.ts` action block uses `options.ci` to decide exit code
  </acceptance_criteria>
  <done>The `--ci` flag is implemented with strict exit code logic.</done>
</task>

<task type="auto">
  <name>Task 2: Verify project-level .mcp.json detection</name>
  <read_first>
    - src/config/paths.ts
    - src/config/detector.ts
  </read_first>
  <files>src/config/paths.ts</files>
  <action>
    Ensure `getProjectLevelPaths()` in `src/config/paths.ts` correctly includes `.mcp.json`.
    Add a manual verification step or test case to ensure that running `mcp-scan` in a directory with a `.mcp.json` file detects it as "Project Local".
  </action>
  <verify>
    <automated>ls .mcp.json 2>/dev/null || echo "Creating mock .mcp.json"; echo '{"mcpServers":{}}' > .mcp.json; node dist/index.js ls | grep "Project Local"; rm .mcp.json</automated>
  </verify>
  <acceptance_criteria>
    - `.mcp.json` in CWD is detected as a tool configuration
    - Detection logic correctly identifies it as "Project Local"
  </acceptance_criteria>
  <done>Project-level `.mcp.json` support is verified.</done>
</task>

<task type="auto">
  <name>Task 3: Version bump to 1.1.0</name>
  <read_first>
    - package.json
  </read_first>
  <files>package.json</files>
  <action>
    Update the `version` field in `package.json` from `1.0.4` to `1.1.0`.
  </action>
  <verify>
    <automated>grep "\"version\": \"1.1.0\"" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` version is exactly `1.1.0`
  </acceptance_criteria>
  <done>Version bumped to 1.1.0.</done>
</task>

</tasks>

<verification>
Run `npm run build` and then `node dist/index.js scan --ci` on a directory with no findings to confirm 0 exit code, and then on a directory with a mock vulnerable config to confirm 1 exit code.
</verification>

<success_criteria>
- `--ci` flag correctly enforces strict exit codes.
- `.mcp.json` project-root detection is solid.
- Project version is 1.1.0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-breadth/03-02-SUMMARY.md`
</output>
