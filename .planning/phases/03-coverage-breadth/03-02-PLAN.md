---
phase: 03-coverage-breadth
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/index.ts
  - src/commands/scan.ts
  - tests/commands/scan.test.ts
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "When `mcp-scan --ci` is run with findings, the process exits with code 1."
    - "When `mcp-scan --ci` is run with no findings, the process exits with code 0."
  artifacts:
    - path: "src/index.ts"
      provides: "`--ci` option for `scan` command"
      contains: "option('--ci'"
    - path: "src/commands/scan.ts"
      provides: "Logic to set `process.exitCode` based on `--ci` and scan results"
      contains: "if (options.ci)", "process.exitCode = 1;"
    - path: "tests/commands/scan.test.ts"
      provides: "Tests for `--ci` flag behavior"
      contains: "exits with 1 on findings", "exits with 0 on no findings"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/scan.ts"
      via: "passing `--ci` option to `scanCommand`"
      pattern: "scanCommand(options);"
    - from: "src/commands/scan.ts"
      to: "process.exitCode"
      via: "conditional assignment based on scan results"
      pattern: "process\.exitCode = 1;"
---

<objective>
Implement the `--ci` flag functionality for the `scan` command and ensure the application exits with the correct status code based on scan findings.

Purpose: Enable `mcp-scan` to be used effectively in CI/CD pipelines, providing clear pass/fail signals. This addresses requirement CI-01.
Output: Modified `src/index.ts`, `src/commands/scan.ts`, and new tests for CI flag behavior.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-coverage-breadth/03-CONTEXT.md
@.planning/phases/03-coverage-breadth/03-RESEARCH.md
@src/index.ts
@src/commands/scan.ts
@tests/commands/scan.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `--ci` option to Commander.js `scan` command in `src/index.ts`</name>
  <files>src/index.ts</files>
  <read_first>
    src/index.ts
    .planning/phases/03-coverage-breadth/03-CONTEXT.md
    .planning/phases/03-coverage-breadth/03-RESEARCH.md
  </read_first>
  <action>
    Modify `src/index.ts` to add the `--ci` option to the `scan` command (per D-03):
    ```typescript
    program.command('scan')
      .description('Scan for MCP server configurations.')
      .option('--ci', 'Exit with code 1 if any findings are detected (Critical to Info).') // Add this line
      .action(async (options) => {
        await scanCommand(options);
      });
    ```
    Ensure `options` is passed to `scanCommand`.
  </action>
  <verify>
    <automated>grep -q ".option('--ci', 'Exit with code 1 if any findings are detected (Critical to Info).')" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.ts` contains the `.option('--ci', ...)` line for the `scan` command.
  </acceptance_criteria>
  <done>
    The `--ci` option is added to the `scan` command in `src/index.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CI exit code logic in `src/commands/scan.ts`</name>
  <files>src/commands/scan.ts</files>
  <read_first>
    src/commands/scan.ts
    src/types/scan-result.ts
    .planning/phases/03-coverage-breadth/03-CONTEXT.md
    .planning/phases/03-coverage-breadth/03-RESEARCH.md
  </read_first>
  <action>
    Modify the `scanCommand` function in `src/commands/scan.ts` to implement the `--ci` flag logic (per D-03):
    1. Update the `scanCommand` function signature to accept `options: { ci?: boolean }`.
    2. After `runScans()` returns the `results: ScanResult[]`, add a conditional block:
       ```typescript
       if (options.ci) {
         const hasSignificantFindings = results.some(result =>
           result.severity === Severity.CRITICAL ||
           result.severity === Severity.HIGH ||
           result.severity === Severity.MEDIUM ||
           result.severity === Severity.LOW ||
           result.severity === Severity.INFO
         );

         if (hasSignificantFindings) {
           process.exitCode = 1; // Set exit code to 1 if any findings
         }
       }
       ```
    Ensure `ScanResult` and `Severity` types are imported from `../types/scan-result`.
  </action>
  <verify>
    <automated>grep -q "options: { ci?: boolean }" src/commands/scan.ts</automated>
    <automated>grep -q "if (options.ci)" src/commands/scan.ts</automated>
    <automated>grep -q "process.exitCode = 1;" src/commands/scan.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/scan.ts`'s `scanCommand` function accepts an `options` object with an optional `ci` property.
    - `src/commands/scan.ts` contains the logic to set `process.exitCode = 1` if `options.ci` is true and there are any findings of severity Critical, High, Medium, Low, or Info.
    - `src/commands/scan.ts` imports `ScanResult` and `Severity`.
  </acceptance_criteria>
  <done>
    CI exit code logic is implemented in `src/commands/scan.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests for `--ci` flag behavior in `tests/commands/scan.test.ts`</name>
  <files>tests/commands/scan.test.ts</files>
  <read_first>
    tests/commands/scan.test.ts
    src/index.ts
    src/commands/scan.ts
    .planning/phases/03-coverage-breadth/03-CONTEXT.md
    .planning/phases/03-coverage-breadth/03-RESEARCH.md
  </read_first>
  <action>
    Add new test cases to `tests/commands/scan.test.ts` to cover the `--ci` flag functionality:
    1. Test that `mcp-scan scan --ci` exits with code `1` when findings (Critical to Info) are present.
    2. Test that `mcp-scan scan --ci` exits with code `0` when no findings are present.
    These tests should mock the `runScans` function or directly invoke `scanCommand` with mock results to control the test scenario. Use `vitest.spyOn` or similar mocking utility to assert `process.exitCode`.
  </action>
  <verify>
    <automated>grep -q "mcp-scan scan --ci" tests/commands/scan.test.ts</automated>
    <automated>npm test -- tests/commands/scan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/commands/scan.test.ts` contains at least two new tests specifically verifying the `--ci` flag's effect on `process.exitCode`.
    - These tests correctly assert the exit code `1` for findings and `0` for no findings.
    - `npm test -- tests/commands/scan.test.ts` passes.
  </acceptance_criteria>
  <done>
    Tests for `--ci` flag behavior are added to `tests/commands/scan.test.ts` and pass.
  </done>
</task>

</tasks>

<verification>
Ensure that `npm run build` completes successfully.
Ensure that `npm test` runs all tests successfully, especially the newly added ones for the `--ci` flag.
Manually verify `node dist/index.js scan --ci` exit codes in a controlled environment (e.g., with mock findings).
</verification>

<success_criteria>
The `--ci` flag correctly causes `mcp-scan` to exit with code 1 if any findings are detected (Critical to Info).
The `--ci` flag causes `mcp-scan` to exit with code 0 if no findings are detected.
All existing tests pass and the build is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-breadth/03-02-SUMMARY.md`
</output>
