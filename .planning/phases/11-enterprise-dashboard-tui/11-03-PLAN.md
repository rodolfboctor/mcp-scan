---
phase: 11-enterprise-dashboard-tui
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: [src/utils/dashboard-ui.ts, src/commands/dashboard.ts, src/index.ts, src/commands/proxy.ts]
autonomous: false
requirements: [UI-03]
must_haves:
  truths:
    - "Real-time scrolling log of proxy traffic is visible"
    - "Proxy monitoring view can be switched to from the main dashboard"
    - "mcp-scan dashboard command starts the TUI"
  artifacts:
    - path: "src/commands/dashboard.ts"
      provides: "Dashboard CLI command"
    - path: "src/utils/dashboard-ui.ts"
      provides: "Proxy monitoring view"
---

<objective>
Implement the real-time proxy monitoring view and integrate the dashboard into the CLI.
Add the `mcp-scan dashboard` command and the `--ui` flag for the proxy.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-enterprise-dashboard-tui/11-02-SUMMARY.md
@src/commands/proxy.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proxy Real-time Monitoring View</name>
  <files>src/utils/dashboard-ui.ts, src/commands/proxy.ts</files>
  <action>
    1. In `src/utils/dashboard-ui.ts`, implement a `ProxyView`:
       - `Log` or `Box`: A scrolling log area for proxy traffic (JSON-RPC messages).
       - `Sparkline`: Request volume over time.
       - `Gauge`: Health/Security score (PII detected vs Total).
    2. Add a callback to `src/utils/dashboard-ui.ts` that can be used to push new proxy logs to the dashboard.
    3. Modify `src/commands/proxy.ts` to accept an optional dashboard update callback (or emit an event).
    4. Ensure JSON-RPC messages are formatted cleanly (truncated if too long) for display in the log area.
  </action>
  <verify>
    <automated>grep -i "ProxyView" src/utils/dashboard-ui.ts</automated>
  </verify>
  <done>Proxy monitor view implemented and wired to receive events</done>
</task>

<task type="auto">
  <name>Task 2: CLI Integration (Dashboard Command & Proxy --ui Flag)</name>
  <files>src/commands/dashboard.ts, src/index.ts, src/commands/proxy.ts</files>
  <action>
    1. Create `src/commands/dashboard.ts` that initializes the `createDashboard` function.
    2. Register the `dashboard` command in `src/index.ts`.
    3. Add a `--ui` flag to the `proxy` command in `src/index.ts`.
    4. If `--ui` is set, `runProxy` should launch the dashboard and pipe traffic logs to it instead of console.log.
  </action>
  <verify>
    <automated>grep "dashboard" src/index.ts</automated>
  </verify>
  <done>CLI integration complete: mcp-scan dashboard and mcp-scan proxy --ui available</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enterprise TUI Dashboard</what-built>
  <how-to-verify>
    1. Build the project: `npm run build`
    2. Run the standalone dashboard: `node dist/index.js dashboard` (ensure audit.log has data)
    3. Verify you can see charts and navigate (H: History, Q: Quit).
    4. Run the proxy with UI: `node dist/index.js proxy --command "ls" --ui` (or a real MCP server command)
    5. Verify you see real-time scrolling logs of "traffic" in the TUI.
  </how-to-verify>
  <resume-signal>Dashboard verified and visuals match "Matrix-style" expectation.</resume-signal>
</task>

</tasks>

<verification>
Full interactive test of the dashboard with live proxy traffic.
</verification>

<success_criteria>
- Real-time proxy monitoring in the terminal.
- Visual command center for scan history and proxy traffic.
- Aesthetics matching ThynkQ brand (Neon Blue on Black).
</success_criteria>

<output>
After completion, create `.planning/phases/11-enterprise-dashboard-tui/11-03-SUMMARY.md`
</output>
