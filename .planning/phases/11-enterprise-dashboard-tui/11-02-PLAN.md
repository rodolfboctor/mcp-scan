---
phase: 11-enterprise-dashboard-tui
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [src/utils/dashboard-ui.ts]
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Dashboard shows scan severity breakdown in a chart"
    - "Dashboard displays the most recent 10 scans in a list"
  artifacts:
    - path: "src/utils/dashboard-ui.ts"
      provides: "Dashboard view logic"
---

<objective>
Populate the TUI dashboard with historical scan data from the audit log.
Implement the "History" view with charts and lists.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-enterprise-dashboard-tui/11-01-SUMMARY.md
@src/utils/audit-logger.ts
@src/commands/history.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scan History Visuals</name>
  <files>src/utils/dashboard-ui.ts</files>
  <action>
    Integrate historical data into the dashboard:
    1. Import `readAuditLog` from `src/utils/audit-logger.js`.
    2. Add a `HistoryView` to the dashboard layout using `blessed-contrib`:
       - `Donut` or `Bar` chart: Severity breakdown across last 20 scans (Sum of critical, high, medium, low).
       - `Table` or `List`: Most recent 10-20 scans (Timestamp, Server name, Findings count).
       - `Box`: "System Stats" summary box showing total lifetime scans.
    3. Implement view-switching logic to show this by default.
  </action>
  <verify>
    <automated>node -e "import { createDashboard } from './dist/utils/dashboard-ui.js'; console.log('View logic check pass');"</automated>
  </verify>
  <done>History view with charts and lists implemented in the dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Implement Key Bindings & Interactivity</name>
  <files>src/utils/dashboard-ui.ts</files>
  <action>
    Add basic interactivity to the TUI:
    - 'Q' or 'Ctrl+C' to exit the dashboard (safely destroy screen).
    - 'R' to refresh data (re-read `audit.log`).
    - Scrolling functionality for the scan list if it overflows.
  </action>
  <verify>
    <automated>grep -i "key.*q" src/utils/dashboard-ui.ts</automated>
  </verify>
  <done>Interactive key bindings implemented</done>
</task>

</tasks>

<verification>
Dashboard should correctly parse audit.log and show the findings.
</verification>

<success_criteria>
- Scan severity findings are visually represented on the dashboard.
- Users can navigate the history view.
</success_criteria>

<output>
After completion, create `.planning/phases/11-enterprise-dashboard-tui/11-02-SUMMARY.md`
</output>
