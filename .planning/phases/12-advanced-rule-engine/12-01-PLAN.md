---
phase: 12-advanced-rule-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/rules.ts, src/utils/rule-engine.ts, src/commands/scan.ts]
autonomous: true
requirements: [ENG-01]
---

<objective>
Implement an advanced rule engine that reads external JSON/YAML rule files to detect patterns without recompiling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/commands/scan.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define Rule Types</name>
  <files>src/types/rules.ts</files>
  <action>
    Create src/types/rules.ts defining CustomRule interface (id, severity, description, pattern/regex, target: env|args|command).
  </action>
  <verify>
    <automated>ls src/types/rules.ts</automated>
  </verify>
  <done>Types defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Rule Engine</name>
  <files>src/utils/rule-engine.ts, tests/utils/rule-engine.test.ts</files>
  <action>
    1. Create src/utils/rule-engine.ts to load rules from ~/.mcp-scan/rules/*.json.
    2. Implement `evaluateCustomRules(server: ResolvedServer, rules: CustomRule[]): Finding[]`.
    3. Update `src/commands/scan.ts` to call this engine during scans.
  </action>
  <verify>
    <automated>npm test tests/utils/rule-engine.test.ts</automated>
  </verify>
  <done>Rule engine functional.</done>
</task>
</tasks>

<verification>
Ensure custom rules can flag arbitrary strings in args or env.
</verification>

<success_criteria>
- Engine parses JSON rules.
- Scans successfully flag hits based on dynamic rules.
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-rule-engine/12-01-SUMMARY.md`
</output>
