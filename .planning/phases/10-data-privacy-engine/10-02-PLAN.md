---
phase: 10-data-privacy-engine
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [src/types/config.ts, src/config/parser.ts, src/commands/proxy.ts, tests/commands/proxy-privacy.test.ts]
autonomous: true
requirements: [PRIV-01]
must_haves:
  truths:
    - "Privacy settings can be configured via .mcp-scan.json policy"
    - "Proxy command applies privacy masking to JSON-RPC traffic"
    - "Performance impact of masking on proxy traffic is < 5ms"
  artifacts:
    - path: "src/types/config.ts"
      provides: "Privacy policy types"
    - path: "src/commands/proxy.ts"
      provides: "PII masking in proxy traffic"
    - path: "tests/commands/proxy-privacy.test.ts"
      provides: "Integration verification of privacy in proxy"
---

<objective>
Integrate the privacy engine into the proxy command and support configuration through the project's policy file.

Purpose: Enable users to control what data is masked during real-time server interaction.
Output: Updated proxy command with masking capabilities and expanded policy support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-data-privacy-engine/10-01-SUMMARY.md
@src/types/config.ts
@src/config/parser.ts
@src/commands/proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Privacy Settings to Policy Config</name>
  <files>src/types/config.ts, src/config/parser.ts</files>
  <behavior>
    - `McpScanPolicy` includes a `privacy` field
    - Privacy options: `maskPii` (boolean), `excludePatterns` (string[]), `customPatterns` (Record<string, string>)
    - `loadPolicy` correctly parses privacy settings from .mcp-scan.json
  </behavior>
  <action>
    Update McpScanPolicy in src/types/config.ts to include privacy-related fields.
    Modify loadPolicy in src/config/parser.ts to ensure these fields are read and typed.
  </action>
  <verify>
    <automated>npm test tests/config/parser.test.ts</automated>
  </verify>
  <done>Privacy configuration supported in policy engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Masking into Proxy Command</name>
  <files>src/commands/proxy.ts, tests/commands/proxy-privacy.test.ts</files>
  <behavior>
    - `runProxy` loads local policy and checks for privacy settings
    - `JsonRpcInterceptor` applies `maskPii` to both CLIENT -> SERVER and SERVER -> CLIENT directions if enabled
    - Masking is only applied to valid JSON payloads; raw lines are logged but not modified
    - Performance: Latency added per JSON-RPC message is measured and remains < 5ms
  </behavior>
  <action>
    Modify runProxy to load the current directory's policy.
    Update JsonRpcInterceptor constructor to accept privacy options.
    Call maskPii in _transform before pushing sanitized data.
    Create an integration test simulating JSON-RPC flow with PII to verify masking.
  </action>
  <verify>
    <automated>npm test tests/commands/proxy-privacy.test.ts</automated>
  </verify>
  <done>Proxy command masks PII in real-time according to configuration.</done>
</task>

</tasks>

<verification>
Run the proxy command with a mock server that exchanges PII and verify that the output seen by the AI client (and sent to the server) is properly masked.
</verification>

<success_criteria>
- `.mcp-scan.json` allows enabling/disabling privacy masking.
- `mcp-scan proxy` correctly masks emails and phone numbers in JSON payloads.
- No regression in proxy functionality (piping works as expected).
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-privacy-engine/10-02-SUMMARY.md`
</output>
