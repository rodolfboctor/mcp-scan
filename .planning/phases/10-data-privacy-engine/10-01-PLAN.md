---
phase: 10-data-privacy-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/data/pii-patterns.ts, src/utils/privacy-engine.ts, tests/utils/privacy-engine.test.ts]
autonomous: true
requirements: [PRIV-01]
must_haves:
  truths:
    - "PII patterns include emails, phone numbers, and common sensitive identifiers"
    - "Privacy engine can mask PII in nested JSON objects"
    - "Masking preserves JSON structure while obscuring sensitive values"
  artifacts:
    - path: "src/data/pii-patterns.ts"
      provides: "PII detection regexes"
    - path: "src/utils/privacy-engine.ts"
      provides: "Masking logic"
    - path: "tests/utils/privacy-engine.test.ts"
      provides: "Verification of masking accuracy"
---

<objective>
Implement the core PII detection patterns and the masking engine that will be used by the proxy to prevent data leaks.

Purpose: Provide a high-performance way to identify and obscure sensitive information in JSON-RPC traffic.
Output: PII regex library and a recursive masking utility with unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/data/secret-patterns.ts
@src/types/scan-result.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define PII Patterns</name>
  <files>src/data/pii-patterns.ts</files>
  <behavior>
    - Email regex matches standard email formats (e.g., user@example.com)
    - Phone number regex matches common formats (e.g., +1-555-0199, (555) 0199)
    - Credit card regex matches 13-16 digit patterns
    - SSN regex matches XXX-XX-XXXX format
    - IP address regex matches IPv4 patterns
  </behavior>
  <action>
    Create src/data/pii-patterns.ts exporting PII_PATTERNS array. 
    Include: Email, Phone, Credit Card, SSN, IPv4, and common physical address keywords.
    Optimize regexes for performance (avoid catastrophic backtracking).
  </action>
  <verify>
    <automated>ls src/data/pii-patterns.ts</automated>
  </verify>
  <done>PII patterns defined and exported as named exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Privacy Engine Masking</name>
  <files>src/utils/privacy-engine.ts, tests/utils/privacy-engine.test.ts</files>
  <behavior>
    - `maskPii(data: any, options: PrivacyOptions): any` recursively traverses objects/arrays
    - Replaces matching strings with masked versions (e.g., "[EMAIL_MASKED]")
    - Supports custom masking placeholders per pattern type
    - Handles non-string values gracefully (skips masking but continues traversal)
    - Performance: Masking a 1KB JSON object with multiple PII hits takes < 1ms
  </behavior>
  <action>
    Create src/utils/privacy-engine.ts with a recursive maskPii function.
    Use PII_PATTERNS for detection. 
    Support an options object to enable/disable specific patterns.
    Create comprehensive tests in tests/utils/privacy-engine.test.ts.
  </action>
  <verify>
    <automated>npm test tests/utils/privacy-engine.test.ts</automated>
  </verify>
  <done>Privacy engine implemented with full test coverage and performance within limits.</done>
</task>

</tasks>

<verification>
Ensure all PII patterns are correctly identified and masked in various JSON structures without corrupting the data.
</verification>

<success_criteria>
- PII_PATTERNS covers at least 5 common PII types.
- maskPii function handles deeply nested JSON.
- Unit tests pass with 100% coverage for privacy-engine.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-privacy-engine/10-01-SUMMARY.md`
</output>
