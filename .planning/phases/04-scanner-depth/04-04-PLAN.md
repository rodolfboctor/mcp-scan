---
phase: 04
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - src/commands/scan.ts
autonomous: true
---

# Plan 04-04: Scanner Orchestration and Final Verification

## Objective
Integrate all new scanners into the main `scan` command orchestration and perform comprehensive verification.

## Requirements
- SCD-01: Implement a prompt injection scanner to detect patterns in tool descriptions and its tests.
- SCD-02: Integrate with OSV.dev API to query for npm package CVEs and surface CRITICAL/HIGH findings and its tests.
- SCD-03: Implement a scanner to flag `.env` files containing secrets in server working directories and its tests.

## Tasks

### 1. Integrate new scanners into `src/commands/scan.ts`.
**<read_first>**
- `src/commands/scan.ts`
- `src/scanners/prompt-injection-scanner.ts`
- `src/scanners/env-leak-scanner.ts`
- `src/scanners/package-scanner.ts`
**</read_first>**
**<action>**
Modify the `runScan` function to:
- Import `scanPromptInjection` from `../scanners/prompt-injection-scanner.js`.
- Import `scanEnvLeak` from `../scanners/env-leak-scanner.js`.
- Call `scanPromptInjection(server)` and `scanEnvLeak(server)` and add their findings to `allFindings`.
- Ensure `scanPackage(server)` (which will include OSV.dev checks) is also called and its findings are added.
**</action>**
**<acceptance_criteria>**
- `src/commands/scan.ts` imports `scanPromptInjection` and `scanEnvLeak`.
- `allFindings` array in `runScan` includes results from `scanPromptInjection(server)` and `scanEnvLeak(server)`.
- `scanPackage(server)` is correctly invoked and its findings are integrated.
**</acceptance_criteria>**

## must_haves
- All three new scanners (Prompt Injection, OSV.dev, .env leak) detect their specified vulnerabilities.
- `mcp-scan` tool does not crash on any valid/invalid input or network issues.
- All new findings have correct severity assigned.
- All scanner implementations are under 200 lines.
- All new scanner functionality is covered by unit tests.
