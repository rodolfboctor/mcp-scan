---
phase: 04-coverage-breadth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/detector.ts
  - src/config/parser.ts
  - src/config/writer.ts
  - tests/config/detector.test.ts
  - tests/config/parser.test.ts
  - tests/config/toml-parser.test.ts
autonomous: true
requirements: [COV-01, COV-02, COV-03, COV-04, COV-05, COV-06, COV-07, COV-08, COV-09]

must_haves:
  truths:
    - "detectTools() returns every tool from getConfigPaths() plus Cline/Roo Code glob results plus project-level paths, deduplicating by resolved path"
    - "parseConfig() handles both JSON and TOML (smol-toml) without throwing on missing files"
    - "atomicWriteConfig() writes via temp file and renames, creating parent directories and timestamped backup"
    - "All tests in tests/config/ pass with npm test"
  artifacts:
    - path: "src/config/detector.ts"
      provides: "detectTools() orchestration — named tools + glob + project-level"
      exports: ["detectTools"]
    - path: "src/config/parser.ts"
      provides: "parseConfig() JSON+TOML, extractServers(), loadPolicy(), loadIgnoreList()"
      exports: ["parseConfig", "extractServers", "loadPolicy", "loadIgnoreList"]
    - path: "src/config/writer.ts"
      provides: "atomicWriteConfig() with backup and temp-rename pattern"
      exports: ["atomicWriteConfig"]
    - path: "tests/config/detector.test.ts"
      provides: "Vitest tests for detectTools() including glob and deduplication"
    - path: "tests/config/parser.test.ts"
      provides: "Vitest tests for JSON parsing and server extraction"
    - path: "tests/config/toml-parser.test.ts"
      provides: "Vitest tests for TOML parsing via smol-toml"
  key_links:
    - from: "src/config/detector.ts"
      to: "src/config/paths.ts"
      via: "imports getConfigPaths, getProjectLevelPaths, getExtensionGlobPaths"
      pattern: "import.*getConfigPaths.*getProjectLevelPaths.*getExtensionGlobPaths"
    - from: "src/config/parser.ts"
      to: "smol-toml"
      via: "import * as toml from 'smol-toml' for .toml config files"
      pattern: "import.*smol-toml"
    - from: "src/config/writer.ts"
      to: "fs.renameSync"
      via: "atomic write via temp file rename, not direct writeFileSync on target"
      pattern: "renameSync\\(tempPath"
---

<objective>
Verify the runtime engine layer of the coverage-breadth phase: the orchestrator (detector.ts), config parser with TOML support (parser.ts), atomic config writer (writer.ts), and their corresponding test suites.

Purpose: These files make detection actually happen at runtime. The parser must handle both JSON and Codex CLI's TOML format. The writer must be safe for config mutation. The detector must deduplicate to avoid double-counting.
Output: Confirmed that all three runtime files function correctly and their test suites pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify detector.ts orchestration and deduplication logic</name>
  <files>src/config/detector.ts, tests/config/detector.test.ts</files>

  <read_first>
    src/config/detector.ts — read fully
    tests/config/detector.test.ts — read fully to understand what the tests assert
  </read_first>

  <action>
    Verify detector.ts implements detectTools() with these behaviors:

    1. Named tool loop — iterates Object.entries(getConfigPaths()) and pushes DetectedTool entries using fs.existsSync() to set exists flag
    2. Deduplication — uses a seenPaths Set<string> with path.resolve() normalization before pushing; skips duplicates
    3. Cline glob — calls getExtensionGlobPaths('Cline', ...) and pushes each result as name:'Cline' (per COV-03)
    4. Roo Code glob — calls getExtensionGlobPaths('Roo Code', ...) and pushes each result as name:'Roo Code' (per COV-04)
    5. Project-level paths — calls getProjectLevelPaths() and only adds paths that exist AND haven't been seen; .mcp.json gets name 'Project .mcp.json' (per COV-09)
    6. Dependency injection — function signature is detectTools(dependencies: { fs, os, process }) with no global os/process calls in function body

    Verify the test file asserts:
    - detectedNames contains 'Zed', 'Continue.dev', 'Amp', 'Plandex', 'ChatGPT Desktop', 'GitHub Copilot', 'Cline', 'Roo Code'
    - A 'Project .mcp.json' entry exists with configPath ending in .mcp.json
    - No path appears more than once (deduplication test)
  </action>

  <verify>
    <automated>
      cd /Users/rodolf/Documents/Code/mcp-scan && npx vitest run tests/config/detector.test.ts 2>&1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "seenPaths" src/config/detector.ts returns a Set deduplication implementation
    - grep "getExtensionGlobPaths.*Cline" src/config/detector.ts shows Cline glob call
    - grep "getExtensionGlobPaths.*Roo Code" src/config/detector.ts shows Roo Code glob call
    - grep "Project .mcp.json" src/config/detector.ts shows label assignment for project-level .mcp.json
    - grep "DetectorDependencies\|dependencies.*fs.*os.*process" src/config/detector.ts confirms injected deps interface
    - npx vitest run tests/config/detector.test.ts exits 0 with all 4 tests passing
  </acceptance_criteria>

  <done>detectTools() correctly orchestrates 13 named tools + Cline/Roo Code globs + project-level paths with deduplication, and all 4 detector tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify parser (JSON+TOML) and writer (atomic) correctness via tests</name>
  <files>src/config/parser.ts, src/config/writer.ts, tests/config/parser.test.ts, tests/config/toml-parser.test.ts</files>

  <read_first>
    src/config/parser.ts — read fully
    src/config/writer.ts — read fully
    tests/config/parser.test.ts — read fully
    tests/config/toml-parser.test.ts — read fully
    tests/fixtures/ — check that valid-config.json and codex-config.toml exist
  </read_first>

  <action>
    Verify parser.ts:
    1. parseConfig() checks path.extname() for .toml — routes to smol-toml for Codex CLI config (per Codex CLI path ~/.codex/config.toml)
    2. TOML parsed config normalizes to { mcpServers } shape — handles both mcpServers and mcp_servers TOML keys
    3. JSON path returns JSON.parse() result directly as McpConfig
    4. On any error (including ENOENT), returns null gracefully (no throws leaking to caller)
    5. extractServers() flattens mcpServers Record into ResolvedServer[] adding name, toolName, configPath fields
    6. loadPolicy() reads .mcp-scan.json from cwd if it exists, returns null if missing
    7. loadIgnoreList() reads .mcp-scan-ignore, strips comments (#) and blank lines

    Verify writer.ts:
    1. atomicWriteConfig() creates parent directory with mkdirSync({ recursive: true }) if missing
    2. Writes to a timestamped temp file first (configPath.tmp-{timestamp})
    3. Creates a timestamped backup if file already exists (configPath.backup-{timestamp})
    4. Uses fs.renameSync(tempPath, configPath) for atomic swap
    5. On failure, cleans up temp file and re-throws

    Verify test fixtures exist at tests/fixtures/valid-config.json and tests/fixtures/codex-config.toml.
  </action>

  <verify>
    <automated>
      cd /Users/rodolf/Documents/Code/mcp-scan && npx vitest run tests/config/parser.test.ts tests/config/toml-parser.test.ts 2>&1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "smol-toml" src/config/parser.ts returns import statement (not jsonwebtoken or js-toml)
    - grep "\.toml" src/config/parser.ts shows the extname branch routing to toml.parse()
    - grep "mcp_servers\|mcpServers" src/config/parser.ts shows both TOML key variants handled
    - grep "renameSync" src/config/writer.ts shows atomic rename pattern (not direct writeFileSync on target)
    - grep "backup-" src/config/writer.ts shows timestamped backup creation
    - ls tests/fixtures/codex-config.toml confirms fixture exists for TOML tests
    - npx vitest run tests/config/parser.test.ts exits 0 with 3 tests passing
    - npx vitest run tests/config/toml-parser.test.ts exits 0 with 2 tests passing
  </acceptance_criteria>

  <done>parseConfig() handles JSON and TOML gracefully, extractServers() produces correct ResolvedServer[], atomicWriteConfig() uses temp-rename pattern with backup, and all parser/TOML tests pass.</done>
</task>

</tasks>

<verification>
Run the complete config test group in one command:

```bash
cd /Users/rodolf/Documents/Code/mcp-scan && npm test -- --reporter=verbose 2>&1 | grep -E "PASS|FAIL|config|detector|parser|toml"
```

All tests in tests/config/ must pass. npm run build must compile without TypeScript errors.
</verification>

<success_criteria>
- detectTools() returns all 13 named tools + Cline/Roo Code + project-level configs
- Path deduplication prevents any config file being scanned twice
- TOML parsing via smol-toml works for Codex CLI config
- Atomic writer uses temp-rename + backup pattern
- All tests in tests/config/ pass (detector: 4 tests, parser: 3 tests, toml-parser: 2 tests)
- npm run build exits 0 with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-breadth/04-02-SUMMARY.md`
</output>
