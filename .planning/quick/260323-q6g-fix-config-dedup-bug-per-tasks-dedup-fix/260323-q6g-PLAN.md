---
phase: quick/260323-q6g-fix-config-dedup-bug-per-tasks-dedup-fix
plan: 260323-q6g
type: execute
wave: 1
depends_on: []
files_modified: [src/config/detector.ts, tests/config/detector.test.ts, package.json]
autonomous: true
requirements: [DEDUP-01]
must_haves:
  truths:
    - "No config file is detected twice under different tool names when running from the home directory"
    - "Running mcp-scan in the project directory returns expected results without false duplicate-server findings"
  artifacts:
    - path: "src/config/detector.ts"
      provides: "Deduplicated tool detection logic"
    - path: "tests/config/detector.test.ts"
      provides: "Regression test for path deduplication"
    - path: "package.json"
      provides: "Version bump to 1.0.4"
  key_links:
    - from: "src/config/detector.ts"
      to: "path.resolve()"
      via: "Deduplication via resolved paths"
---

<objective>
Fix the config deduplication bug where running `mcp-scan` from the user's home directory (`~`) detects the same config file twice, causing false `duplicate-server` findings.

Purpose: Prevent false positive security findings by ensuring each unique configuration file is processed only once.
Output: Updated `src/config/detector.ts`, `tests/config/detector.test.ts`, and `package.json`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@TASKS-DEDUP-FIX.md

<interfaces>
From src/config/detector.ts:
```typescript
export function detectTools(): DetectedTool[];
```

From src/config/paths.ts:
```typescript
export function getConfigPaths(): Record<string, string>;
export function getProjectLevelPaths(): string[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bug Fix Implementation and Regression Test</name>
  <files>src/config/detector.ts, tests/config/detector.test.ts</files>
  <action>
    - In `src/config/detector.ts`, import `path` and update `detectTools()` to deduplicate paths using `path.resolve()` and a `seenPaths` Set.
    - Resolve both global and project-local paths before comparison.
    - Skip project-local paths if they have already been detected as a global tool.
    - Update `tests/config/detector.test.ts` to include the regression test: 'should not detect same config path under multiple tool names'.
    - Also add `import { detectTools } from '../../src/config/detector.js';` and `import path from 'path';` to the test file.
  </action>
  <verify>
    <automated>npm run build && npm test tests/config/detector.test.ts</automated>
  </verify>
  <done>
    `detectTools()` now returns unique resolved paths and the new test case passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Version Bump and Final Verification</name>
  <files>package.json</files>
  <action>
    - Bump the version in `package.json` from `1.0.3` to `1.0.4`.
    - Perform a clean build with `npm run build`.
    - Run all tests with `npm test`.
    - Perform manual verification: Run `node dist/index.js` from the home directory (mock or real if possible) to confirm "Project Local" duplicates are gone.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>
    `package.json` is at v1.0.4 and all tests pass.
  </done>
</task>

</tasks>

<verification>
Manual verification:
1.  Navigate to your home directory: `cd ~`
2.  Run the build scan: `node [repo-path]/dist/index.js`
3.  Confirm that there are no false `duplicate-server` findings for files like `~/.codex/config.toml`.
</verification>

<success_criteria>
- [ ] `src/config/detector.ts` deduplicates paths using `path.resolve()`.
- [ ] `tests/config/detector.test.ts` includes a test for deduplication.
- [ ] `package.json` version is updated to `1.0.4`.
- [ ] `npm test` passes all tests.
- [ ] Manual check confirms no duplicate findings from home directory.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-q6g-fix-config-dedup-bug-per-tasks-dedup-fix/260323-q6g-SUMMARY.md`
</output>
